#!/bin/bash

PARAMS="$1 $2 $3"
if [[ $# -eq 1 ]]; then
	PNAME=$1
	PARAMS="-pName $PNAME"
fi
if [[ ($# -eq 2) && (($1 -eq "-pname") || ($1 -eq "-pName")) ]]; then
	PNAME=$2
	PARAMS= "-pName $PNAME"
fi

java -cp ./netamtp.jar com.netazoic.netamtp.MTPSync $PARAMS
